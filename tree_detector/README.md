Tree Detector
=============

`tree.launch` assumes its data is in `laser_hover/demo/mounted.bag`.

For details, see the report.

If you look at the `hough` branch of this under git, you'll see a prototype version that uses Hough circle detection that wasn't discussed in the report.
